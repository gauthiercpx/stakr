-r requirements.txt
black==26.1.0
flake8
isort
pytest
pytest-cov
# Required by fastapi.testclient.TestClient. Previously pulled in implicitly by
# `fastapi[standard]`, which the runtime requirements no longer install.
httpx==0.28.1
